// WCAG 2.5.5 (Target Size, AAA) recommends interactive controls be at least 44x44 CSS
// px on touch devices, so a finger-sized tap doesn't miss or hit the wrong control.
export const MIN_TAP_TARGET_PX = 44;
