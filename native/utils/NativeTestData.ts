// Placeholder — this domain reuses ui/utils/Users.ts for login credentials and has no
// static test data of its own yet. Replace with real per-concern files the first time
// this domain needs its own data (see ui/utils/ for the pattern).
export {};
