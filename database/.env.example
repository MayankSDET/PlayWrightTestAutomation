# Optional: path to a SQLite file for DB tests. Defaults to an in-memory database (:memory:) if unset.
# Set to a real path (e.g. ./data/test.db) to test against a persistent SQLite file instead.
DB_FILE_PATH=:memory:
