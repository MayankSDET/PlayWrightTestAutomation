// Placeholder for database-domain test data. Add one file per concern here (e.g. SeedUsers.ts)
// the same way ui/utils/Users.ts, CheckoutData.ts, and Products.ts are split, once this
// domain has real static data to hold.
export {};
