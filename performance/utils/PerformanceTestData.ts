// Placeholder for performance-domain test data. Add one file per concern here the same way
// ui/utils/Users.ts, CheckoutData.ts, and Products.ts are split, once this domain has real
// static data to hold (e.g. per-page budget overrides beyond performanceThresholds.ts).
export {};
